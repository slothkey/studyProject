<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.heyi.demo">


    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <activity android:name=".coordinateLayout.CoordinateActivity">

        </activity>

        <activity android:name=".coordinateLayout.CoordinateContainerActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        </activity>

        <activity android:name=".rv.RvActivity">
        </activity>

        <activity android:name=".calendar.CalanderActivity">

        </activity>

        <activity android:name=".calendar.CalendarUIActivity">

        </activity>

        <activity android:name=".annotation.AnnotationActivity">

        </activity>

        <activity android:name=".testApt.TestAptActivity">

        </activity>

        <activity android:name=".gestureLock.GestureLockActivity">

        </activity>

        <activity android:name=".signature.SignatureActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>